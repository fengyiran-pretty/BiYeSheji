# Web应用快速参考指南

## 一句话说明

**Web应用界面的所有功能都在 `web_app/app.py` 文件中实现，通过Gradio框架创建用户界面，支持图像上传、实时分类和Grad-CAM可视化。**

## 快速启动

```bash
# 最简单的启动方式
python web_app/app.py

# 演示模式（不需要模型）
python demo_web_app.py
```

访问: http://localhost:7860

## 核心文件

| 文件 | 功能 | 代码行数 |
|------|------|---------|
| `web_app/app.py` | 主应用程序 | 292行 |
| `launch_web_app.py` | 启动脚本 | 177行 |
| `demo_web_app.py` | 演示脚本 | 210行 |

## 关键类和函数

### 1. SkinLesionClassifier类
**位置**: `web_app/app.py` 第28-154行  
**功能**: 封装模型加载、预测和可视化

```python
classifier = SkinLesionClassifier(
    model_path='checkpoints/best_model.pth',
    model_type='resnet50',
    num_classes=2,
    class_names=['良性', '恶性']
)
```

### 2. classify_image()函数
**位置**: `web_app/app.py` 第180-198行  
**功能**: Gradio界面与分类器的桥梁

```python
def classify_image(image: np.ndarray):
    predicted_class, confidences, visualization = classifier.predict(image)
    return confidences, visualization
```

### 3. create_gradio_interface()函数
**位置**: `web_app/app.py` 第201-255行  
**功能**: 创建完整的Gradio界面

```python
interface = gr.Interface(
    fn=classify_image,
    inputs=gr.Image(type="numpy"),
    outputs=[gr.Label(), gr.Image()]
)
```

### 4. main()函数
**位置**: `web_app/app.py` 第258-292行  
**功能**: 程序入口，启动服务器

```python
interface.launch(
    server_name="0.0.0.0",
    server_port=7860
)
```

## 工作流程（5步）

```
1. 用户上传图像 
   ↓
2. classify_image() 接收
   ↓
3. classifier.predict() 处理
   ↓
4. 返回置信度 + 热力图
   ↓
5. Gradio显示结果
```

## 界面组件

### 输入
- **图像上传** (`gr.Image`)
  - 支持: JPG, PNG
  - 格式: numpy数组
  - 任意尺寸

### 输出
- **分类结果** (`gr.Label`)
  - 显示: 类别 + 置信度
  - 格式: {"良性": 0.85, "恶性": 0.15}
  
- **Grad-CAM** (`gr.Image`)
  - 显示: 热力图叠加原图
  - 红色: 高关注区域
  - 蓝色: 低关注区域

## 命令行参数

```bash
python web_app/app.py \
  --model_path checkpoints/best_model.pth \  # 模型路径
  --model_type resnet50 \                     # 模型类型
  --port 7860 \                               # 端口号
  --share                                     # 公共链接
```

## 自定义选项

### 修改端口
```python
# web_app/app.py 第267行
parser.add_argument('--port', type=int, default=8080)
```

### 修改类别
```python
# web_app/app.py 第170行
class_names = ['类别1', '类别2', '类别3']
```

### 修改标题
```python
# web_app/app.py 第205行
title = "你的自定义标题"
```

## 代码位置速查

| 功能 | 文件 | 行号 |
|------|------|------|
| 模型加载 | app.py | 58-68 |
| 图像预处理 | app.py | 87-100 |
| 模型推理 | app.py | 118-127 |
| Grad-CAM生成 | app.py | 136-154 |
| Gradio接口 | app.py | 180-198 |
| 界面创建 | app.py | 201-255 |
| 服务器启动 | app.py | 284-288 |

## 常见问题

### Q: 如何启动？
A: `python web_app/app.py` 或 `python launch_web_app.py`

### Q: 没有模型怎么办？
A: 运行 `python demo_web_app.py` 使用演示模式

### Q: 如何访问？
A: 打开浏览器访问 http://localhost:7860

### Q: 如何停止？
A: 按 Ctrl+C

### Q: 如何分享？
A: 添加 `--share` 参数

## 依赖包

核心依赖（requirements.txt中）:
- gradio
- torch
- numpy
- opencv-python
- albumentations

## 扩展阅读

详细文档:
- `WEB应用使用说明.md` - 完整使用指南
- `界面展示图解.md` - 界面结构和代码详解
- `README.md` - 项目整体说明

## 技术支持

遇到问题？查看:
1. 日志输出 (终端)
2. 浏览器控制台 (F12)
3. `WEB应用使用说明.md` 的常见问题部分

---

**最后更新**: 2026年2月
**版本**: 1.0
